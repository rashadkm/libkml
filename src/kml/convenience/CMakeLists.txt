file(GLOB CONVENIENCE_SRCS "${CMAKE_CURRENT_SOURCE_DIR}/*.cc" )
file(GLOB CONVENIENCE_INCS "${CMAKE_CURRENT_SOURCE_DIR}/*.h")
set(LIBKML_SRCS ${LIBKML_SRCS} ${CONVENIENCE_SRCS} PARENT_SCOPE)
set(LIBKML_INCS ${LIBKML_SRCS} ${CONVENIENCE_INCS} PARENT_SCOPE)
