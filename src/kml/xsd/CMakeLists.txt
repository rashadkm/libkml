file(GLOB XSD_SRCS "${CMAKE_CURRENT_SOURCE_DIR}/*.cc" )
file(GLOB XSD_INCS "${CMAKE_CURRENT_SOURCE_DIR}/*.h")
set(LIBKML_SRCS ${LIBKML_SRCS} ${XSD_SRCS} PARENT_SCOPE)
set(LIBKML_INCS ${LIBKML_SRCS} ${XSD_INCS} PARENT_SCOPE)

